apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}-worker-service-metrics
  labels:
    app: worker-service
    release: {{ .Release.Name }}
spec:
  ports:
    - name: metrics
      port: 9090
      targetPort: 9090
  selector:
    app: worker-service
    release: {{ .Release.Name }}
